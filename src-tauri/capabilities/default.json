{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Peekaboo capability set — only the window commands the WebView invokes (hide, set-focus, drag). All other window control and the panic global shortcut run from the Rust core, where the ACL does not apply.",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:webview:allow-create-webview",
    "core:webview:allow-set-webview-size",
    "core:webview:allow-set-webview-position",
    "core:webview:allow-webview-close"
  ]
}
